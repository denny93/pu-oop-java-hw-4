import java.awt.*;

public class GPSCordinates extends  BaseModel{
    public GPSCordinates(Color color) {
        super(color);
    }
    public GPSCordinates(Color color , int mode) {
        super(color, mode);
    }
}
