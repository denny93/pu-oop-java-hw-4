import java.awt.*;

public class StartCordinates extends  BaseModel{
    public StartCordinates(Color color) {
        super(color);
    }
    public StartCordinates(Color color , int mode) {
        super(color, mode);
    }
}
